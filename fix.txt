diff --git a/lib/composer-components/addon/components/action-button-with-permission/component.js b/lib/composer-components/addon/components/action-button-with-permission/component.js
index bab8136329..e725ef1174 100644
--- a/lib/composer-components/addon/components/action-button-with-permission/component.js
+++ b/lib/composer-components/addon/components/action-button-with-permission/component.js
@@ -10,7 +10,6 @@ import $ from 'jquery';
 export default Component.extend({
     layout: layout,
     permissionChecker: service('permission-checker'),
-    observedArrays: A([]),
     tagName: 'span',
     model: null,
     /**
@@ -39,6 +38,7 @@ export default Component.extend({
         let component = this;
         let buttons = [];
         let actions = component.get('apiActionButtons');
+        console.log("Leo - actions "+actions.length);
         if (!isEmpty(actions)) {
             actions = actions.filter(x=> x.isVisible !== false);
             actions.forEach(function(action) {
@@ -52,6 +52,7 @@ export default Component.extend({
                     action: () => {},
                     optionStyle: action.optionStyle
                 };
+                console.log("Leo - actions "+JSON.stringify(parent));
 
                 if (groups && groups.length > 0) {
                     let _grps = [];
@@ -131,6 +132,11 @@ export default Component.extend({
         return isBtnVisible;
     },
 
+    init() {
+        this._super(...arguments);
+        this.set('observedArrays', A([]));
+    },
+
     didInsertElement(){
         this._setUpToolTip(false,true);
     },
diff --git a/lib/composer-components/addon/components/actions-button/component.js b/lib/composer-components/addon/components/actions-button/component.js
index 195835b007..a2cfbef615 100644
--- a/lib/composer-components/addon/components/actions-button/component.js
+++ b/lib/composer-components/addon/components/actions-button/component.js
@@ -1,7 +1,6 @@
 import { computed } from '@ember/object';
 import Component from '@ember/component';
 import layout from 'composer-components/components/actions-button/template';
-import { A } from '@ember/array';
 
 export default Component.extend({
 
@@ -9,7 +8,7 @@ export default Component.extend({
 
     classNames: ['btn-group'],
 
-    classNamesBindings: A(['isClicked:open']),
+    classNameBindings: ['open'],
 
     //As this actions btn used mostly in maintenance table, so it refers to the model of clicked table row in most cases.
     //This can also be the object need to be modified in option's action.
@@ -21,13 +20,21 @@ export default Component.extend({
     optionsArray: null,
 
     isBtnDisabled: true,
-    optionStyleClass: computed('optionStyle', function(){
-        if(this.get('optionStyle') === 'primary'){
+    open: false,
+    optionStyleClass: computed('optionStyle', function () {
+        if (this.get('optionStyle') === 'primary') {
             return 'btn-primary';
-        } else if(this.get('optionStyle') === 'info'){
+        } else if (this.get('optionStyle') === 'info') {
             return 'btn-info';
         } else {
             return 'btn-default';
         }
-    })
+    }),
+
+    actions: {
+        toggleOpen() {
+            this.toggleProperty('open');
+            return false;
+        }
+    }
 });
diff --git a/lib/composer-components/addon/components/actions-button/template.hbs b/lib/composer-components/addon/components/actions-button/template.hbs
index 1f8dcddd92..9bca9cfebb 100644
--- a/lib/composer-components/addon/components/actions-button/template.hbs
+++ b/lib/composer-components/addon/components/actions-button/template.hbs
@@ -1,5 +1,5 @@
 <a class="btn btn-sm {{optionStyleClass}} btn-default {{if isBtnDisabled 'disabled'}}">{{btnName}}</a>
-<a href="#" class="btn btn-sm {{optionStyleClass}} btn-default dropdown-toggle {{if isBtnDisabled 'disabled'}}" data-toggle="dropdown" aria-expanded="false">
+<a href="#" class="btn btn-sm {{optionStyleClass}} btn-default dropdown-toggle {{if isBtnDisabled 'disabled'}}" data-toggle="dropdown" aria-expanded="false" {{action "toggleOpen"}}>
     <span class="caret"/>
 </a>
 <ul class="dropdown-menu dropdown-menu-right">
